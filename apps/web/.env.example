# API Configuration
# Leave empty or unset to use relative path /api (recommended for production with nginx proxy)
# Set to http://localhost:3000 for local development
VITE_API_URL=http://localhost:3000
